@using shopthoitrang.Models
@model List<Loaisanpham>

<h2></h2>
<ul class="nav flex-column">
    @foreach (var lh in Model)
    {
        <li>
            @Html.ActionLink(@lh.Tenloai, "HTTheoLoaiHang", "Home", new { id = @lh.Maloai }, new { @class = "nav-link text-dark" })
        </li>
    }
</ul>
